%h2.text-center.mb-4 タスク一覧

.text-center.mb-4
  = link_to 'タスクを追加', new_admins_task_path, class: 'btn btn-outline-secondary'

.row.justify-content-center
  .col-md-8
    - if @tasks.exists?
      %table.table.table-bordered.align-middle.text-center
        %thead
          %tr
            %th.w-10 ID
            %th タスク名
            %th カテゴリー
            %th 点数
            %th 操作
        %tbody
          - @tasks.each do |task|
            %tr
              %td= task.id
              %td= task.name
              %td= task.category_text
              %td= task.point
              %td
                = link_to '詳細', admins_task_path(task), class: 'btn btn-sm btn-outline-secondary me-2'
                = link_to '編集', edit_admins_task_path(task), class: 'btn btn-sm btn-outline-warning me-2'
                = button_to '削除',
                            admins_task_path(task),
                            method: :delete,
                            data: { turbo_confirm: '本当に削除しますか？' },
                            form: { class: 'd-inline' },
                            class: 'btn btn-sm btn-outline-danger'
    - else
      %p.text-center カテゴリーがありません。
